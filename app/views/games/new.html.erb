<div class="px-3">
  <h1> <%=  @group.name %> </h1>
  <%= simple_form_for [@group, @game] do |f| %>
    <%= f.association :match, label: "Choose a match", label_method: :teams %>
    <%= f.submit 'Create Game', class: "btn btn-greenstart" %>
  <% end %>
</div>
